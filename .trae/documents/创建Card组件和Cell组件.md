我将按照项目的组件结构和编码规范，创建Card组件和Cell组件。每个组件将包含以下文件：

### Card组件
1. **md-card.vue**：组件的模板、脚本和样式
2. **type.ts**：包含CardProps和CardEmits接口
3. **index.ts**：导出组件和类型

### Cell组件
1. **md-cell.vue**：组件的模板、脚本和样式
2. **type.ts**：包含CellProps和CellEmits接口
3. **index.ts**：导出组件和类型

### 组件功能规划

#### Card组件
- 支持自定义卡片标题
- 支持自定义卡片内容
- 支持自定义卡片底部操作区域
- 支持不同的卡片类型（默认、带边框、带阴影等）
- 支持自定义卡片样式

#### Cell组件
- 支持左侧图标
- 支持标题和描述
- 支持右侧内容（文本、图标或自定义内容）
- 支持点击事件
- 支持不同的单元格类型（默认、带箭头、带开关等）
- 支持自定义单元格样式

### 实现步骤
1. 创建Card组件的文件结构和代码
2. 创建Cell组件的文件结构和代码
3. 确保组件符合项目的编码规范
4. 确保组件类型定义正确
5. 确保组件导出格式正确

我将按照上述计划创建这两个组件。